# 机洗功能实现总结

## ✅ 已完成的修改

### 1. ListSteps.vue (列表页步骤组件)
- ✅ 添加 `isMachineWash` 计算属性用于判断机洗模式
- ✅ 更新 `normalizedSteps` 逻辑：
  - 当 sequence_no 为 3 的下一步是 7 时，识别为机洗模式
  - 机洗模式下，只显示前3步 + 1步机洗（合并了消毒、终末漂洗、干燥）
  - 普通模式下，显示完整的 6 步流程

### 2. WorkflowSteps.vue (详情页步骤组件)
- ✅ 添加 `isMachineWash` 计算属性
- ✅ 更新 `normalizedSteps` 逻辑，支持机洗模式显示

### 3. WorkflowList.vue (工作流列表页)
- ✅ 添加 `STEP_NAMES_BY_SEQ` 步骤序号映射（包含 '7': '机洗'）
- ✅ 添加 `isMachineWash` 函数判断机洗模式
- ✅ 更新 `getStepName` 函数支持机洗步骤名称
- ✅ 更新模拟数据，包含机洗和普通模式的测试案例

### 4. 测试数据 (mock/data.js)
- ✅ 添加 `mockMachineWashData` 机洗模式示例
- ✅ 添加 `mockNormalWashData` 普通模式示例

## 🎯 功能特性

### 自动判断逻辑
```
漂洗(seq=3) -> 下一步seq=4  ➡️  普通模式 (6步)
漂洗(seq=3) -> 下一步seq=7  ➡️  机洗模式 (4步: 测漏+清洗+漂洗+机洗)
```

### 步骤显示
- **初始状态**：显示所有6步
- **机洗检测后**：动态变为4步（前3步 + 机洗）
- **计时**：机洗作为单独一步计时

### 数据格式
**普通模式：** sequence_no: 1, 2, 3, 4, 5, 6  
**机洗模式：** sequence_no: 1, 2, 3, 7

## 🧪 测试方法

### 1. 启动开发服务器
```bash
npm run dev
```

### 2. 查看效果
- 访问工作流列表页面
- 查看"张华（机洗）"和"赵磊（机洗）"任务
- 对比普通任务和机洗任务的步骤显示差异

### 3. 验证点
- ✅ 机洗任务只显示 4 个步骤
- ✅ 第 4 步显示为"机洗"
- ✅ 计时器显示"机洗"
- ✅ 普通任务仍显示完整 6 步

## 📝 使用说明

### 后端数据格式要求
```javascript
// 机洗模式
{
  steps: [
    { sequence_no: '1', status: 'finished', step_type: 'leak_test' },
    { sequence_no: '2', status: 'finished', step_type: 'cleaning' },
    { sequence_no: '3', status: 'finished', step_type: 'rinsing' },
    { sequence_no: '7', status: 'doing', step_type: 'machine_wash' }
  ]
}

// 普通模式
{
  steps: [
    { sequence_no: '1', status: 'finished', step_type: 'leak_test' },
    { sequence_no: '2', status: 'finished', step_type: 'cleaning' },
    { sequence_no: '3', status: 'finished', step_type: 'rinsing' },
    { sequence_no: '4', status: 'doing', step_type: 'disfection' },
    { sequence_no: '5', status: 'pending', step_type: 'final_rinsing' },
    { sequence_no: '6', status: 'pending', step_type: 'drying' }
  ]
}
```

## ⚠️ 注意事项
1. `sequence_no` 必须是字符串类型
2. 机洗的 `sequence_no` 固定为 `'7'`
3. 前三步的 `sequence_no` 必须是 `'1'`, `'2'`, `'3'`
4. 系统自动判断，无需额外配置
